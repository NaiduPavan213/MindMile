# Copy this file to .env and replace values with your settings
# Example local MongoDB (you can keep this for local development if you run MongoDB locally)
MONGO_URI=mongodb://127.0.0.1:27017/mindmile
PORT=5000
# JWT_SECRET used by auth routes (if implemented)
JWT_SECRET=change_me_in_production
